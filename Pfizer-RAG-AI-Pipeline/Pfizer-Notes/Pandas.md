
Cleaning Data: identifies errors, missing values, outliers, ect. 

Transforming: convert data into usable format though encoding, scaling, and feature engineering

Integration: combines multiple datasets into a unified format

splitting: divides data into training, validation and testing sets, helps prevent bias by ensuring the model learns and geeralizes well, 

structured vs non-structured:

structured: CSVs, database, spreadhseets,
unstructured: PDfs, require extra extraction steps, formatting inconsistencies, 


Data must be cleaned, structured and formatted for AI models to work accurately

cleaning: fix missing valuesm duplicated, errors,
transforming: convert text, standardize fromats,
integration: merge multiple datasets into a usable structure,
splitting divide into training and test sets for machine learning

clean data may invovle using an average age for data missing age value

DataFrame:
 like a spreadsheet with collumns and rows

series:
a single collumn spreadsheet





